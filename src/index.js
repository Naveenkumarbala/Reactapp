import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
// import App from "./App"
const root = ReactDOM.createRoot(document.getElementById('root'));
//render send data to html element with the id root.
root.render(
    <>
        <App/>
    </>
);